import React from 'react'
import { View, Text } from 'react-native'

export default function ShowRoom() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Show Room here</Text>
        </View>
    )
}
